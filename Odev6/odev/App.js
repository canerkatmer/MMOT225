import React from 'react';
import {
SafeAreaView,StyleSheet, ScrollView, View, Text, StatusBar,
} from 'react-native';

import L from './Al';

const App: () => React$Node = () => {
  return (
    
    <SafeAreaView>
    <L/>
    </SafeAreaView>
  );
};
export default App;